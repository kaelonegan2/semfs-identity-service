# Build Context

Role: `runtime-compatibility` build note.

This folder explains deployment expectations for a runtime adapter. It does not contain build scripts because this identity repo is not a runnable app.

Runtime implementation repositories may provide CLI scripts, workflow deployment, container images, queues, webhook endpoints, or hosted services. Those belong to the runtime project, not this identity repo.
