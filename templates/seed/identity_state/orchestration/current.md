# Current Orchestration

Role: `authoritative` seed orchestration summary.

Current route field: `decision.routing.next`.

Active map: `identity_state/orchestration/dispatch-map.json`.

The runtime must select only active seed routes in `seed_runtime_available`.

Mature business routes for lead qualification, estimates, work packets, maintenance follow-up, payment, scheduling, and live sends are inactive future capabilities.
