# Seed Stages

Role: `guidance`.

Seed stages are defined in `stages.json` and explained in `seed-stage-plan.md`.

The runtime should verify trust, understand intent, select an active seed route, dispatch the selected seed agent, validate the facet, hydrate the runtime contract, persist safe artifacts, and then review or close out.

Legacy mature route names are inactive in seed mode.
