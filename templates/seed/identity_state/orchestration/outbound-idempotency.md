# Outbound Idempotency

Role: `runtime-compatibility`.

This repo does not enable live sending. It defines the audit surface a runtime must satisfy before any final external send in a future mature mode.

Seed mode blocks live external sends.
