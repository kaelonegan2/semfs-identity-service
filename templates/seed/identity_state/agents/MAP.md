# Agents Map

Role: `guidance`.

This directory describes seed agents and candidate future agents.

Active agent truth lives in `identity_state/registries/agents.json`.

No future candidate can run until registry, prompt, output contract, facet policy, runtime support, eval coverage, and owner approval exist.
