# Seed Vector Examples

Role: `example`.

No vector memory exists at initialization.

Example owner onboarding upsert:

```json
{
  "namespace": "owner-onboarding-summaries",
  "summary": "Owner approved drafting identity purpose, profile assumptions, and first knowledge candidates. External sends remain blocked.",
  "approval_required": false
}
```

Example capability gap upsert:

```json
{
  "namespace": "capability-gap-history",
  "summary": "Intake is blocked until audience, offering catalog, external-send policy, and commitment boundaries are approved.",
  "approval_required": false
}
```
