# Seed Conformance Fixtures

Role: `example`.

The previous mid-state fixtures for spring cleanup leads and final price requests have been removed from the seed branch because those routes are not active in `seed_runtime_available`.

Seed conformance is covered by:

- `evals/questions.md`
- `evals/expected-answers.md`
- `examples/seed-state/README.md`

Future fixtures should test only active seed routes unless explicitly marked `inactive_future_reference`.
